from torch import nn
import torch.functional as F
import torch

'''
This is an implementation of a variational auto-encoder structure. It consists of:
    1. The encoder, taking the form of a Feed-Forward Network (FFN) 
    2. The decoder, taking the form of a Feed-Forward Network (FFN)
The encoder maps onto a multivariate distribution
'''

class encoder(torch.nn):

    def __init__():
        super.__init__()
        
